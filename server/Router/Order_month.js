const express = require('express')
var router = express.Router();
const con = require('../connection');

router.get('/:id' , (req , reś)=>{
    try {
        let sql = "";
        
        
    } 
    catch (error) {
        
    }
   
})